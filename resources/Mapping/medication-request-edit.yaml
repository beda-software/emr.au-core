resourceType: Mapping
id: medication-request-edit
type: FHIRPath
body:
  "{% assign %}":
    - medicationRequestId: "{{ %QuestionnaireResponse.answers('medicationRequestId') }}"
    - patientId: "{{ %QuestionnaireResponse.answers('patientId') }}"
    - patientName: "{{ %QuestionnaireResponse.answers('patientName') }}"
    - requesterReference: "{{ %QuestionnaireResponse.answers('requester-reference') }}"
    - requesterDisplay: "{{ %QuestionnaireResponse.answers('requester-display') }}"
    - medication: "{{ %QuestionnaireResponse.answers('medication') }}"
    - reason: "{{ %QuestionnaireResponse.answers('reason') }}"
    - dosage: "{{ %QuestionnaireResponse.answers('dosage') }}"
    - status: "{{ %QuestionnaireResponse.answers('status').code }}"
    - intent: "{{ %QuestionnaireResponse.answers('intent').code }}"
    - authoredOn: "{{ %QuestionnaireResponse.answers('authored-on') }}"

  resourceType: Bundle
  type: transaction
  entry:
    - request:
        method: PUT
        url: "/MedicationRequest/{{ %medicationRequestId }}"
      resource:
        resourceType: MedicationRequest
        id: "{{ %medicationRequestId }}"
        meta:
          profile:
            - http://hl7.org.au/fhir/core/StructureDefinition/au-core-medicationrequest
        status: "{{ %status }}"
        intent: "{{ %intent }}"
        medicationCodeableConcept:
          text: "{{ %medication }}"
        subject:
          reference: "Patient/{{ %patientId }}"
          display: "{{ %patientName }}"
        authoredOn: "{{ %authoredOn }}"
        requester:
          reference: "{{ %requesterReference }}"
          display: "{{ %requesterDisplay }}"
        reasonCode:
          - text: "{{ %reason }}"
        dosageInstruction:
          - text: "{{ %dosage }}"
